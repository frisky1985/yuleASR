/**
 * @file Rte_Type.h
 * @brief RTE Type Definitions
 * @generated by RTE Generator
 */

#ifndef RTE_TYPE_H
#define RTE_TYPE_H

#include "Std_Types.h"

/* Standard RTE Types */
typedef uint8 Rte_ComponentInstanceType;
typedef uint16 Rte_InstanceHandleType;

/* Signal Status Types */
typedef enum {
    RTE_E_OK = 0,
    RTE_E_INVALID = 1,
    RTE_E_COM_BUSY = 2,
    RTE_E_NEVER_RECEIVED = 3,
    RTE_E_MAX_AGE_EXCEEDED = 4,
    RTE_E_UNCONNECTED = 5
} Rte_StatusType;

{% for interface in interfaces %}
/* Interface: {{ interface.name }} */
{% if interface.type == 'SENDER-RECEIVER' %}
{% for de in interface.data_elements %}
typedef {{ de.type }} {{ interface.name }}_{{ de.name }}_Type;
{% endfor %}
{% endif %}

{% endfor %}

#endif /* RTE_TYPE_H */

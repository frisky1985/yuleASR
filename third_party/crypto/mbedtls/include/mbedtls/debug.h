/*==================================================================================================
 * debug.h - mbedTLS debug stub (yuleASR)
 *================================================================================================*/
#ifndef MBEDTLS_DEBUG_H
#define MBEDTLS_DEBUG_H

#include <stddef.h>

void mbedtls_debug_set_threshold(int threshold);

#endif /* MBEDTLS_DEBUG_H */

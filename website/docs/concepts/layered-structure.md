# 分层结构

了解 AutoSAR 的分层架构设计。

## 层次结构

```
┌───────────────┐
│  应用层 (ASW)   │
├───────────────┤
│  运行时环境(RTE)│
├───────────────┤
│  服务层         │
├───────────────┤
│  ECU 抽象层    │
├───────────────┤
│  微控制驱动层   │
└───────────────┘
```
